Drop SSS1 product images in this folder to show them in the gallery.

Supported extensions: `.avif`, `.jpeg`, `.jpg`, `.png`, `.webp`.

Images are sorted by filename, so prefix files with numbers to control the order:

```text
01-main.jpg
02-front.jpg
03-angle.jpg
```
